#include <bits/stdc++.h>

using namespace std;

const int r = 5009;
int dp[r][r];
int tab[r];

int main()
{
    int n;
    cin >> n;
    for(int i = 0; i < n; i++)
        cin >> tab[i];
    for(int l = 
}