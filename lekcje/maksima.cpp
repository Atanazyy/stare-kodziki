#include <bits/stdc++.h>

using namespace std;

const int r = 1e6;
long long drz[r];
int roz;

int main()
{
    int n, q;
    cin >> n >> q;
    roz = ceil(log2(n));
    roz = 1 << roz;
    roz--;
    for(int i = 1; i <= n; i++)
    {
        
    }
}