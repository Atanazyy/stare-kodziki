#include <bits/stdc++.h>

using namespace std;

const int r = 2e4 + 9;
int dp[r];
pair <int, int> tab[209];

int main()
{
    int n;
    cin >> n;
    for(int i = 1; i <= n; i++)
        cin >> tab[i].first;
    for(int i = 1; i <= n; i++)
        cin >> tab[i].second;
    for(int i = 1; i <= n; i++)
    {
        for(int j = 0; j <= ; j++)
        {

        }
    }
}