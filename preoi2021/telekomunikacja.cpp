#include <bits/stdc++.h>

using namespace std;

struct mas
{
    int a;
    int s;
};

map <int, mas> mapa;

int main()
{
    int n, m;
    cin >> n >> m;
    while(m--)
    {
        char z;
        cin >> z;
        if(z == 'P')
        {
            int x, s, a;
            cin >> x >> s >> a;
        }
        else if(z == 'U')
        {
            int x;
            cin >> x;
        }
    }
}